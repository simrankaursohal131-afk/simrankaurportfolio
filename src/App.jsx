import Portfolio from './Portfolio.jsx'

/**
 * App.jsx — Root component.
 *
 * All application logic lives in Portfolio.jsx.
 * Add any future global providers (Router, ThemeContext, etc.) here.
 */
export default function App() {
  return <Portfolio />
}
